# AGENTS.md — AI 协作手册

> 本文件是给 AI 编码助手的工作手册，多数 AI 工具会自动读取，无需人工提示。
> 项目总览与一级目录索引见 [README.md](./README.md)。
> 本手册只写**通用协作规则**；项目特有的内容（技术栈、模块边界、命令）在各节占位，引入实际项目后填写。

## 0. 项目定位

`<项目名：一句话业务定位。后端 <技术栈>，前端 <技术栈>。核心业务：<主数据流>。>`

> 占位未填说明仍是骨架状态，按 [README.md §3](./README.md) 引入实际项目。

---

## 1. 结构演进规则（开放性）

- 本骨架**不是刚性模板**：开发中可按需增删、合并目录与文件。
- **改结构必同步文档**：一级目录变动更新 [README.md §2 索引](./README.md)；子目录变动更新该目录 README；本手册相关描述过期一并更新。
- 引入开源框架时**尊重框架自身结构**，不强行改组目录去套骨架；骨架索引适配实际情况。
- docs / sql / test / deploy 是通配外围结构，按各自 README 使用与增减。

---

## 2. 阅读边界（先读这条）

> 原则：**只读用到的部分，其余当不存在**。阅读量每大一分，产出质量降一分。

- 动手前先确认当前任务涉及哪些模块，只读该模块及其依赖的公共部分。
- 框架源码中未启用 / 已注销的模块不读不处理；搜索命中时，去已启用模块找等价用法。
- 框架中标注「不可修改」的核心 / 基座模块**禁改源码**，只使用它提供的能力。

---

## 3. 改动边界

- **业务功能** → 只写业务模块，不侵扰框架公共代码。
- **公共能力扩展**（认证 / 权限 / 工具）→ 写对应公共层，注意依赖方向，禁止业务被公共层反向依赖。
- 以框架现有写法为样板，不发明新写法；不裸用基础组件重造已有封装。
- 对开源框架源码做业务定制时，改动尽量集中、可升级，并在 docs/plan 变更记录中登记。

---

## 4. 领域与权限模型

`<填写本项目的数据权限 / 业务范围模型。若存在两层以上独立权限，分别说明各自作用对象与判定规则，并标注「两层模型不同，不要套用」。>`

---

## 5. 开发流程（每次任务按此走）

1. **读本手册** + 相关目录 README，建立心智模型。
2. **定位文档**：docs/prd（需求）→ docs/plan（进度）→ 验收指南（怎么验、谁能用）。
3. **确定范围**：列清单——后端 / 前端 / SQL / 文档。
4. **开发**：以已启用的现有代码为样板。
5. **自测**：起服务，按验收指南端到端验证。
6. **更新文档**（最易漏）：计划勾选 + 变更记录；字段 / 菜单 / 权限 / 流程变动同步相关文档。

---

## 6. 构建与命令

```powershell
# 后端
<构建与启动命令>

# 前端
<安装与启动命令>
```

- API Base：`<地址>`；认证方式：`<如 Bearer Token>`。
- SQL 脚本由开发提供到 [sql/](./sql/README.md)，**用户在数据库执行后告知「已执行」**，不自动跑。
- 默认只做编译验证，不安装到运行实例（除非用户明确要求）。

---

## 7. 编码硬规则摘要

> 若 docs/rules/ 已有项目规范，写代码前先读；开源框架开发时**框架自身约定优先**于通用规则。

- 分层清晰；VO 严格校验；业务异常统一类型 + 错误码。
- 敏感字段脱敏；统计优先 SQL 聚合；批量查询防 N+1。
- 文本输入加长度上限；字典字段用下拉 + 标签渲染。

---

## 8. 文档索引

| 需求 | 位置 |
|------|------|
| 需求 / 字段 / 流程 | [docs/prd/](./docs/prd/README.md) |
| 接口契约 / 错误码 | [docs/api/](./docs/api/README.md) |
| 进度 / 变更记录 / 验收指南 | [docs/plan/](./docs/plan/README.md) |
| 编码规范 / 自测清单 | [docs/rules/](./docs/rules/README.md) |
| 文档详细索引 | [docs/README.md](./docs/README.md) |

> **单一维护原则**：每个事实只在一处维护，其它位置只链接不复制。
